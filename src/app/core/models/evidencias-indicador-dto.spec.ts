import { EvidenciasIndicadorDTO } from './evidencias-indicador-dto';

describe('EvidenciasIndicadorDTO', () => {
  it('should create an instance', () => {
    expect(new EvidenciasIndicadorDTO()).toBeTruthy();
  });
});
